  
server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/adpa1982/jwt_spring/
          search-paths: config-data
  application:
    name: config-service